import React, { ReactNode } from "react";
interface HTTPSLinkProps {
    decoratedText: string;
    children: ReactNode;
}
export default function HTTPSLink({ decoratedText, children }: HTTPSLinkProps): React.JSX.Element;
export {};
